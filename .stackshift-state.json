{
  "detection_type": "generic",
  "route": "brownfield",
  "implementation_framework": "speckit",
  "config": {
    "spec_output_location": ".",
    "build_location": ".",
    "target_stack": "TypeScript 5.x + pnpm workspaces + Zod + Vitest + Gherkin (existing)",
    "brownfield_mode": "standard",
    "transmission": "manual",
    "spec_thoroughness": "specs_plus_plans"
  },
  "_notes": "Defaults chosen non-interactively. This repo is the @libar-dev/architect-* package family — a pnpm monorepo with its own mature spec system (architect/specs/, ADRs, formal-spec/). Brownfield/speckit chosen as safe defaults; redirect if a different path fits."
}
